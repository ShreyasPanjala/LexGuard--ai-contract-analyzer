/**
 * controllers/index.js
 *
 * Barrel export for all route controllers.
 * Import and export controllers here as they are implemented.
 *
 * Example:
 *   const { analyzeContract } = require('./contract.controller');
 *   module.exports = { analyzeContract };
 */

'use strict';

module.exports = {};
