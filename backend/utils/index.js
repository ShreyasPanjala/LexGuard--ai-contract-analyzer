/**
 * utils/index.js
 *
 * Barrel export for shared utility functions.
 *
 * Example:
 *   const { formatResponse } = require('./response.util');
 *   module.exports = { formatResponse };
 */

'use strict';

module.exports = {};
