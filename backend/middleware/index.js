/**
 * middleware/index.js
 *
 * Barrel export for custom Express middleware.
 *
 * Example:
 *   const { errorHandler } = require('./error.middleware');
 *   module.exports = { errorHandler };
 */

'use strict';

module.exports = {};
